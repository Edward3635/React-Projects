import { Dispatch } from "react";
import { TodoAction } from "../../types/todo";

const fetchTodos = ():any=> async (dispatch: Dispatch<TodoAction>) =>{
	try {
		
	} catch (error) {
		
	}
};
export default fetchTodos;