import React from 'react';

function App() {

	return (
		<div></div>
		// <AuthContext.Provider value={{ isAuth, setIsAuth, isLoading }}>
		// 	<BrowserRouter>
		// 		<Navbar />
		// 		<AppRouter />
		// 	</BrowserRouter>
		// </AuthContext.Provider>
	);
}

export default App;
